process {

    publishDir = { "${params.outdir}/${task.process.tokenize(':')[-1].tokenize('_')[0].toLowerCase()}" }

    withName: PARTIS_PARTITION {
        ext.args = ''
    }

    withName: PARTIS_PARTITION_EXTARGS {
        ext.args = '--extra-annotation-columns consensus_seq'
    }

}
